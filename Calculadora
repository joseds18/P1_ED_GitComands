
git checkout -b agregar-multiplicacion
git checkout trunk
git merge agregar-raiz
git checkout -b agregar-potencia
git checkout trunk
git merge agregar-potencia
git push origin trunk
git tag v1.0
git push origin v1.0
